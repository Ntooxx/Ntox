version: "3.8"

services:
  ntox:
    build: .
    container_name: ntox
    ports:
      - "3000:3000"
    volumes:
      - ntox_data:/data
      - ./:/workspace
    environment:
      - NTOX_DIR=/data
    restart: unless-stopped

volumes:
  ntox_data:
